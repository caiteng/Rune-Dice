import Phaser from 'phaser';import type { Die } from '../core/types';import { RUNE_EMOJI } from '../data/runes';
export class DiceView{rect:Phaser.GameObjects.Rectangle;txt:Phaser.GameObjects.Text;constructor(scene:Phaser.Scene,public die:Die,x:number,y:number,cb:()=>void){this.rect=scene.add.rectangle(x,y,62,62,0x1f2937).setStrokeStyle(2,0x9ca3af).setInteractive();this.txt=scene.add.text(x,y,'',{fontSize:'30px'}).setOrigin(0.5);this.rect.on('pointerdown',cb);}render(){this.txt.setText(RUNE_EMOJI[this.die.value]);this.rect.setStrokeStyle(3,this.die.locked?0x22c55e:0x9ca3af);this.rect.setFillStyle(this.die.blocked?0x7f1d1d:0x1f2937)}}
