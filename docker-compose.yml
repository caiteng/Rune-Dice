services:
  rune-dice:
    build:
      context: .
      args:
        APP_VERSION: ${APP_VERSION:-local}
    image: rune-dice:latest
    container_name: rune-dice
    restart: unless-stopped
    ports:
      - "9999:9999"
    volumes:
      - /opt/rune-dice/data:/data
